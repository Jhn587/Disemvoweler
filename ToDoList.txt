ToDo List



Test - See if regex is faster than a for loop and switch statements with StringBuilder -- Looks like regex is slower than switch statements


Project Runs well right now
StringBuilder is implemented, should allow faster/more efficient runtimes with long strings